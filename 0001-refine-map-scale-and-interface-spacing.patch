From 51a2234473e35fe8930a33021a26cc8e54931d0e Mon Sep 17 00:00:00 2001
From: Basileus Layout Pass <layout-pass@example.local>
Date: Sun, 3 May 2026 20:03:18 +0000
Subject: [PATCH] Refine map scale and interface spacing

---
 LAYOUT_BREATHING_PASS.md    |  61 ++++
 assets/style.css            | 243 ++++++++++++----
 layout-breathing-pass.patch | 552 ++++++++++++++++++++++++++++++++++++
 3 files changed, 800 insertions(+), 56 deletions(-)
 create mode 100644 LAYOUT_BREATHING_PASS.md
 create mode 100644 layout-breathing-pass.patch

diff --git a/LAYOUT_BREATHING_PASS.md b/LAYOUT_BREATHING_PASS.md
new file mode 100644
index 0000000..3c5fcec
--- /dev/null
+++ b/LAYOUT_BREATHING_PASS.md
@@ -0,0 +1,61 @@
+# Layout Breathing Pass
+
+## Diagnostic
+
+The previous responsive pass made the game usable on narrow screens, but the main layout still had three pressure points:
+
+1. **Map canvas pressure**
+   - The map SVG was allowed to fill all available horizontal and vertical space.
+   - The decorative frame around the map area was larger than the rendered map, which made the canvas feel oversized even when the actual map content was smaller.
+   - The bottom tab rail had too little reserved vertical space, so the player rectangles felt pinned to the bottom edge.
+
+2. **Interface column pressure**
+   - The sidebar was still too narrow on desktop and medium screens.
+   - Panels were stacked edge-to-edge with little separation, so dashboard, history, and action controls visually merged.
+   - Several rows used dense flex layouts where labels, values, buttons, and badges competed for the same line.
+
+3. **Rigid component sizing**
+   - Repeated card grids used small minimum column widths.
+   - Appointment, market, finance, candidate, deployment, mercenary, and revocation controls did not have enough spacing to wrap cleanly.
+   - Mobile map height consumed too much of the viewport relative to the interface below it.
+
+## Applied plan
+
+1. **Constrain the map instead of letting it dominate**
+   - Preserve the map SVG aspect ratio.
+   - Remove the oversized decorative frame around the map area.
+   - Give the SVG itself the visible border/shadow so the visible canvas is the map canvas.
+   - Increase the bottom reservation for player tabs.
+   - Slightly reduce mobile map rows to leave more room for interface content.
+
+2. **Give the interface column more width and internal rhythm**
+   - Increase desktop sidebar width dynamically with `clamp()`.
+   - Raise the single-column breakpoint from 900px to 980px so medium screens stop compressing both map and sidebar.
+   - Add sidebar padding and gaps between dashboard/history/action panels.
+   - Increase shared panel padding.
+
+3. **Relax cramped controls and information rows**
+   - Increase min widths for dashboard stats, finance cards, market/gift items, and appointment columns.
+   - Add more spacing in list rows, fold sections, history cards, theme chips, choice grids, and revocation controls.
+   - Let candidate rows, mercenary rows, deployment rows, and mobile value badges wrap more naturally.
+   - Convert deployment rows to a responsive grid where appropriate.
+
+4. **Keep the pass code-light**
+   - No game logic changes.
+   - No rendering algorithm changes.
+   - Main changes are CSS layout constraints and spacing tokens.
+
+## QA performed
+
+- CSS brace balance checked.
+- Full project tests passed with `npm test`.
+- No game-state or multiplayer logic was touched.
+
+## Manual visual QA targets
+
+Check these viewport classes after deployment:
+
+- Desktop wide: 1440×900 and 1920×1080.
+- Medium desktop/tablet landscape: 1024×768 and 1180×820.
+- Phone portrait: 390×844 and 430×932.
+- Firefox desktop and Firefox Android, focusing on map sizing, wrapping controls, and panel spacing.
diff --git a/assets/style.css b/assets/style.css
index dc1ea94..403e0cd 100644
--- a/assets/style.css
+++ b/assets/style.css
@@ -83,8 +83,12 @@
 
   /* Responsive shell */
   --topbar-height: 52px;
-  --sidebar-width: clamp(340px, 28vw, 420px);
-  --main-panel-pad: clamp(12px, 1.4vw, 20px);
+  --sidebar-width: clamp(410px, 34vw, 560px);
+  --map-aspect: 297 / 210;
+  --map-reserve-bottom: calc(76px + env(safe-area-inset-bottom));
+  --map-shell-pad: clamp(14px, 1.8vw, 24px);
+  --sidebar-gap: clamp(12px, 1.3vw, 18px);
+  --main-panel-pad: clamp(16px, 1.7vw, 24px);
   --touch-target: 44px;
   --game-vh: 100vh;
 
@@ -160,7 +164,7 @@ body::before {
   z-index: 1;
   display: grid;
   grid-template-rows: var(--topbar-height) minmax(0, 1fr);
-  grid-template-columns: minmax(0, 1fr) minmax(320px, var(--sidebar-width));
+  grid-template-columns: minmax(460px, 1fr) minmax(390px, var(--sidebar-width));
   grid-template-areas:
     "topbar topbar"
     "map    sidebar";
@@ -305,23 +309,14 @@ body::before {
 
 /* Elegant inner border frame for the map */
 #mapArea::before {
-  content: '';
-  position: absolute;
-  inset: 10px 10px 56px 10px;
-  border: 1px solid rgba(168,116,32,0.35);
-  border-radius: 20px;
-  box-shadow:
-    inset 0 0 0 1px rgba(255,245,210,0.5),
-    0 0 30px rgba(168,116,32,0.08);
-  pointer-events: none;
-  z-index: 1;
+  content: none;
 }
 
 #mapContainer {
   width: 100%;
   height: 100%;
   min-width: 0;
-  padding: clamp(10px, 1.4vw, 18px) clamp(10px, 1.4vw, 18px) calc(58px + env(safe-area-inset-bottom));
+  padding: var(--map-shell-pad) var(--map-shell-pad) var(--map-reserve-bottom);
   display: flex;
   align-items: center;
   justify-content: center;
@@ -330,11 +325,12 @@ body::before {
 
 .game-map {
   display: block;
-  width: 100%;
+  width: auto;
   height: 100%;
-  min-width: 0;
-  max-width: 100%;
+  aspect-ratio: var(--map-aspect);
+  max-width: min(100%, 1120px);
   max-height: 100%;
+  border: 1px solid rgba(100,70,20,0.14);
   border-radius: 16px;
   background: linear-gradient(160deg, rgba(230,240,248,0.85) 0%, rgba(220,210,180,0.80) 100%);
   box-shadow:
@@ -352,8 +348,8 @@ body::before {
   left: 0;
   right: 0;
   display: flex;
-  gap: 4px;
-  padding: 6px max(8px, env(safe-area-inset-left)) calc(7px + env(safe-area-inset-bottom)) max(8px, env(safe-area-inset-right));
+  gap: 8px;
+  padding: 10px max(14px, env(safe-area-inset-left)) calc(12px + env(safe-area-inset-bottom)) max(14px, env(safe-area-inset-right));
   overflow-x: auto;
   overflow-y: hidden;
   scrollbar-width: none;
@@ -366,12 +362,12 @@ body::before {
 /* Player tabs reuse the cartouche grammar at a larger scale: thick role
    outline, dynasty-color fill, parchment ink, gold-leaf inner hairline. */
 .player-tab {
-  flex: 1 0 clamp(132px, 19vw, 190px);
+  flex: 1 0 clamp(156px, 18vw, 220px);
   min-width: 0;
   display: flex;
   align-items: center;
   gap: var(--sp-2);
-  padding: 6px 12px 7px;
+  padding: 8px 14px 9px;
   border: 4px solid var(--role-outline-color, var(--role-color, var(--empire-border)));
   border-bottom-width: 0;
   border-radius: var(--r-sm) var(--r-sm) 0 0;
@@ -455,9 +451,12 @@ body::before {
   flex-direction: column;
   overflow-y: auto;
   overflow-x: hidden;
+  gap: var(--sidebar-gap);
+  padding: var(--sidebar-gap);
   background:
     linear-gradient(180deg, var(--parch-0) 0%, var(--parch-1) 100%);
   border-left: 1px solid var(--parch-3);
+  scrollbar-gutter: stable;
   min-width: 0;
   min-height: 0;
   box-shadow: -8px 0 24px rgba(80,50,10,0.10);
@@ -492,8 +491,11 @@ body::before {
 .sidebar-panel {
   display: flex;
   flex-direction: column;
-  background: transparent;
+  background: rgba(255,252,240,0.54);
   overflow: visible;
+  border: 1px solid rgba(168,116,32,0.16);
+  border-radius: var(--r-lg);
+  box-shadow: var(--shadow-card);
 }
 
 .sidebar-panel-head {
@@ -598,7 +600,7 @@ body::before {
     ),
     linear-gradient(180deg, rgba(255,248,220,0.18) 0%, rgba(20,8,0,0.18) 100%);
   border: 4px solid var(--role-outline-color, var(--role-color, var(--empire-border)));
-  border-radius: 3px;
+  border-radius: var(--r-lg);
   box-shadow:
     inset 0 0 0 1px rgba(240,200,90,0.45),
     inset 0 1px 0 rgba(255,250,228,0.18),
@@ -740,14 +742,14 @@ body::before {
 
 .dashboard-stats {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(88px, 1fr));
+  grid-template-columns: repeat(auto-fit, minmax(112px, 1fr));
   gap: var(--sp-2);
   margin-bottom: var(--sp-3);
 }
 
 .stat {
   flex: 1;
-  padding: var(--sp-2) var(--sp-2);
+  padding: 10px var(--sp-3);
   border-radius: var(--r-sm);
   text-align: center;
   background: rgba(0,0,0,0.1);
@@ -807,7 +809,7 @@ body::before {
   box-shadow: var(--shadow-card);
 }
 
-.fold-section + .fold-section { margin-top: var(--sp-2); }
+.fold-section + .fold-section { margin-top: var(--sp-3); }
 .fold-section.focused { border-color: var(--parch-3); }
 
 .fold-summary {
@@ -854,7 +856,7 @@ body::before {
 .dashboard-list {
   display: flex;
   flex-direction: column;
-  gap: 6px;
+  gap: 8px;
 }
 
 .dashboard-list.compact { margin-top: var(--sp-2); }
@@ -865,8 +867,8 @@ body::before {
   align-items: flex-start;
   justify-content: space-between;
   gap: var(--sp-2);
-  padding: 7px 10px;
-  border-radius: var(--r-sm);
+  padding: 9px 12px;
+  border-radius: var(--r-md);
   background: rgba(255,252,240,0.85);
   border: 1px solid rgba(168,116,32,0.14);
 }
@@ -942,7 +944,7 @@ body::before {
 /* Finance cards */
 .finance-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(min(100%, 136px), 1fr));
+  grid-template-columns: repeat(auto-fit, minmax(min(100%, 158px), 1fr));
   gap: var(--sp-2);
   margin-bottom: var(--sp-2);
 }
@@ -1035,7 +1037,7 @@ body::before {
 .theme-market,
 .gift-options {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(min(100%, 148px), 1fr));
+  grid-template-columns: repeat(auto-fit, minmax(min(100%, 176px), 1fr));
   gap: var(--sp-2);
 }
 
@@ -1094,7 +1096,7 @@ body::before {
 
 
 /* ── Appointment blocks ──────────────────────────────────────────── */
-.appointment-block { margin-bottom: var(--sp-3); }
+.appointment-block { margin-bottom: var(--sp-4); }
 .appointment-block.done { opacity: 0.65; }
 .appointment-block.done .appt-label { color: var(--gold-2); }
 
@@ -1109,12 +1111,12 @@ body::before {
 .appt-form {
   display: flex;
   flex-direction: column;
-  gap: var(--sp-1);
+  gap: var(--sp-2);
 }
 
 .appt-grid {
   display: grid;
-  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
+  grid-template-columns: repeat(2, minmax(150px, 1fr));
   gap: var(--sp-2);
   align-items: start;
 }
@@ -1142,7 +1144,7 @@ body::before {
 .appt-select,
 .revoke-select {
   width: 100%;
-  padding: 7px 10px;
+  padding: 9px 12px;
   background: var(--parch-0);
   border: 1px solid var(--parch-2);
   border-radius: var(--r-sm);
@@ -1218,6 +1220,8 @@ body::before {
 
 .army-office {
   display: flex;
+  flex: 1 1 160px;
+  min-width: 0;
   flex-direction: column;
   gap: 2px;
 }
@@ -1293,7 +1297,7 @@ body::before {
   align-items: center;
   flex-wrap: wrap;
   gap: var(--sp-2);
-  padding: 7px 10px;
+  padding: 9px 12px;
   background: var(--parch-0);
   border: 1px solid var(--parch-2);
   border-radius: var(--r-sm);
@@ -1355,8 +1359,9 @@ body::before {
 .merc-row {
   display: flex;
   align-items: center;
+  flex-wrap: wrap;
   gap: var(--sp-2);
-  padding: 6px 10px;
+  padding: 9px 12px;
   background: var(--parch-0);
   border: 1px solid var(--parch-2);
   border-radius: var(--r-sm);
@@ -1368,6 +1373,7 @@ body::before {
 .merc-controls {
   margin-left: auto;
   display: flex;
+  flex: 0 1 auto;
   align-items: center;
   gap: 7px;
 }
@@ -1407,13 +1413,14 @@ body::before {
 .merc-total .over-budget { color: var(--crimson-1); font-weight: 600; }
 
 /* Candidate buttons — share the cartouche grammar at row scale. */
-.candidate-grid { display: flex; flex-direction: column; gap: var(--sp-1); }
+.candidate-grid { display: flex; flex-direction: column; gap: var(--sp-2); }
 
 .candidate-btn {
   display: flex;
   align-items: center;
+  flex-wrap: wrap;
   gap: var(--sp-2);
-  padding: 7px 12px 8px;
+  padding: 10px 14px 11px;
   background-color: var(--player-color, var(--parch-3));
   background-image:
     repeating-linear-gradient(
@@ -1463,6 +1470,8 @@ body::before {
 
 .current-basileus-tag {
   margin-left: auto;
+  white-space: normal;
+  text-align: right;
   font-size: 9px;
   color: var(--gold-2);
   font-family: var(--font-display);
@@ -1691,7 +1700,7 @@ body::before {
 .history-panel-list {
   display: flex;
   flex-direction: column;
-  gap: var(--sp-2);
+  gap: var(--sp-3);
   padding: var(--sp-3);
 }
 
@@ -1855,7 +1864,7 @@ body::before {
 }
 
 /* Theme chips */
-.dashboard-themes { display: flex; flex-wrap: wrap; gap: var(--sp-1); }
+.dashboard-themes { display: flex; flex-wrap: wrap; gap: var(--sp-2); }
 .theme-chip {
   padding: 2px 9px;
   background: var(--parch-0);
@@ -2224,7 +2233,7 @@ body::before {
 .province-choice-grid {
   display: flex;
   flex-wrap: wrap;
-  gap: var(--sp-1);
+  gap: var(--sp-2);
 }
 
 .player-choice-btn,
@@ -2265,7 +2274,7 @@ body::before {
 /* Revocation choice grid — same cartouche pattern as player/province
    choice grids. Each button wraps one or two cartouches that describe
    what the revocation will affect. */
-.revocation-shell { display: flex; flex-direction: column; gap: var(--sp-2); margin-top: 4px; }
+.revocation-shell { display: flex; flex-direction: column; gap: var(--sp-3); margin-top: 6px; }
 .revocation-group { display: flex; flex-direction: column; gap: 4px; }
 .revocation-group-label {
   font-family: var(--font-display);
@@ -2278,7 +2287,7 @@ body::before {
 .revocation-choice-grid {
   display: flex;
   flex-wrap: wrap;
-  gap: var(--sp-1);
+  gap: var(--sp-2);
 }
 .revocation-choice-btn {
   display: inline-flex;
@@ -2655,6 +2664,91 @@ body::before {
 
 
 
+
+/* ── Breathing layout pass ──────────────────────────────────────────
+   Keeps the SVG canvas locked to the map aspect ratio, widens the usable
+   interface column, and gives repeated controls/cards consistent space. */
+@media (min-width: 981px) {
+  #mapContainer {
+    justify-content: center;
+  }
+
+  .sidebar-panel + .sidebar-panel,
+  #historyPanel + #actionPanel {
+    margin-top: 0;
+  }
+}
+
+.sidebar-panel-head {
+  min-height: var(--touch-target);
+}
+
+.sidebar-panel-badge,
+.dashboard-list-value,
+.score-breakdown,
+.current-basileus-tag {
+  max-width: 48%;
+}
+
+.fold-body,
+.court-section,
+.orders-section,
+.resolution-section,
+.phase-header,
+.court-actions,
+.orders-actions,
+.resolution-actions {
+  min-width: 0;
+}
+
+.deploy-row {
+  display: grid;
+  grid-template-columns: minmax(140px, 1fr) auto auto;
+  align-items: center;
+}
+
+.deploy-toggle {
+  min-width: min(100%, 150px);
+}
+
+.toggle-btn,
+.merc-btn,
+.btn-recruit,
+.btn-dismiss,
+.btn-seat-kind,
+#btnLeaveRoom,
+.inline-button {
+  min-height: 36px;
+}
+
+.market-item,
+.gift-item,
+.candidate-btn,
+.deploy-row,
+.merc-row,
+.history-entry-card,
+.dashboard-list-row,
+.dashboard-opinion-row,
+.finance-card,
+.theme-chip,
+.score-row {
+  min-width: 0;
+}
+
+@media (max-width: 1180px) and (min-width: 981px) {
+  #app {
+    grid-template-columns: minmax(420px, 1fr) minmax(380px, var(--sidebar-width));
+  }
+
+  .deploy-row {
+    grid-template-columns: minmax(0, 1fr);
+  }
+
+  .deploy-toggle {
+    width: 100%;
+  }
+}
+
 /* ── Responsive Ergonomics & Firefox/Mobile Compatibility ─────────────
    The desktop game stays two-column. Narrow screens become a scrollable
    map-first document so the sidebar no longer crushes the map. */
@@ -2707,13 +2801,13 @@ select,
 input,
 summary { touch-action: manipulation; }
 
-@media (max-width: 1120px) {
-  :root { --sidebar-width: clamp(320px, 34vw, 380px); }
+@media (max-width: 1180px) {
+  :root { --sidebar-width: clamp(380px, 38vw, 460px); --main-panel-pad: 16px; }
   .game-title { letter-spacing: 3px; }
   .phase-badge { padding-inline: 10px; }
 }
 
-@media (max-width: 900px) {
+@media (max-width: 980px) {
   body {
     overflow: auto;
     min-height: var(--game-vh);
@@ -2722,7 +2816,7 @@ summary { touch-action: manipulation; }
 
   #app {
     grid-template-columns: minmax(0, 1fr);
-    grid-template-rows: auto clamp(340px, 54svh, 580px) auto;
+    grid-template-rows: auto clamp(330px, 50svh, 540px) auto;
     grid-template-areas:
       "topbar"
       "map"
@@ -2749,7 +2843,7 @@ summary { touch-action: manipulation; }
   }
 
   #mapArea {
-    min-height: 340px;
+    min-height: 320px;
     border-bottom: 1px solid var(--parch-3);
   }
 
@@ -2758,6 +2852,7 @@ summary { touch-action: manipulation; }
     border-left: 0;
     border-top: 1px solid var(--parch-3);
     box-shadow: 0 -8px 24px rgba(80,50,10,0.08);
+    padding: var(--main-panel-pad);
   }
 
   #playerDashboard,
@@ -2778,7 +2873,7 @@ summary { touch-action: manipulation; }
   }
 
   #app {
-    grid-template-rows: auto clamp(320px, 50svh, 520px) auto;
+    grid-template-rows: auto clamp(300px, 46svh, 500px) auto;
   }
 
   #topBar {
@@ -2806,14 +2901,14 @@ summary { touch-action: manipulation; }
     white-space: normal;
   }
 
-  #mapArea::before { inset: 8px 8px 54px; border-radius: 14px; }
-  #mapContainer { padding: 10px 10px calc(56px + env(safe-area-inset-bottom)); }
+  #mapArea::before { inset: 10px 10px calc(68px + env(safe-area-inset-bottom)); border-radius: 14px; }
+  #mapContainer { padding: 10px 10px calc(72px + env(safe-area-inset-bottom)); }
   .game-map { border-radius: 12px; }
 
-  #playerTabBar { gap: 5px; }
+  #playerTabBar { gap: 7px; padding-top: 9px; }
   .player-tab {
-    flex-basis: 145px;
-    padding: 7px 10px;
+    flex-basis: 156px;
+    padding: 8px 10px;
     font-size: 11px;
     border-width: 3px;
     border-bottom-width: 0;
@@ -2931,7 +3026,7 @@ summary { touch-action: manipulation; }
 }
 
 @media (max-width: 480px) {
-  #app { grid-template-rows: auto clamp(300px, 48svh, 480px) auto; }
+  #app { grid-template-rows: auto clamp(280px, 44svh, 440px) auto; }
   .game-title { flex-basis: 100%; }
   #roundDisplay { margin-right: auto; }
   .phase-badge { padding: 4px 8px; }
@@ -2952,6 +3047,42 @@ summary { touch-action: manipulation; }
   .scoring-panel { padding: var(--main-panel-pad); }
 }
 
+
+@media (max-width: 720px) {
+  #sidebar {
+    gap: var(--sp-3);
+  }
+
+  .sidebar-panel {
+    border-radius: var(--r-md);
+  }
+
+  .sidebar-panel-badge,
+  .dashboard-list-value,
+  .score-breakdown,
+  .current-basileus-tag {
+    max-width: 100%;
+  }
+
+  .deploy-row {
+    grid-template-columns: 1fr;
+  }
+
+  .merc-controls,
+  .deploy-toggle {
+    justify-content: stretch;
+  }
+
+  .merc-btn,
+  .merc-count {
+    flex: 1;
+  }
+
+  .market-cost {
+    margin-left: 0;
+  }
+}
+
 /* ── Scrollbar (global) ──────────────────────────────────────────── */
 ::-webkit-scrollbar { width: 7px; }
 ::-webkit-scrollbar-track { background: var(--parch-1); }
diff --git a/layout-breathing-pass.patch b/layout-breathing-pass.patch
new file mode 100644
index 0000000..da2b58f
--- /dev/null
+++ b/layout-breathing-pass.patch
@@ -0,0 +1,552 @@
+diff --git a/mnt/data/style_before.css b/assets/style.css
+index dc1ea94..403e0cd 100644
+--- a/mnt/data/style_before.css
++++ b/assets/style.css
+@@ -83,8 +83,12 @@
+ 
+   /* Responsive shell */
+   --topbar-height: 52px;
+-  --sidebar-width: clamp(340px, 28vw, 420px);
+-  --main-panel-pad: clamp(12px, 1.4vw, 20px);
++  --sidebar-width: clamp(410px, 34vw, 560px);
++  --map-aspect: 297 / 210;
++  --map-reserve-bottom: calc(76px + env(safe-area-inset-bottom));
++  --map-shell-pad: clamp(14px, 1.8vw, 24px);
++  --sidebar-gap: clamp(12px, 1.3vw, 18px);
++  --main-panel-pad: clamp(16px, 1.7vw, 24px);
+   --touch-target: 44px;
+   --game-vh: 100vh;
+ 
+@@ -160,7 +164,7 @@ body::before {
+   z-index: 1;
+   display: grid;
+   grid-template-rows: var(--topbar-height) minmax(0, 1fr);
+-  grid-template-columns: minmax(0, 1fr) minmax(320px, var(--sidebar-width));
++  grid-template-columns: minmax(460px, 1fr) minmax(390px, var(--sidebar-width));
+   grid-template-areas:
+     "topbar topbar"
+     "map    sidebar";
+@@ -305,23 +309,14 @@ body::before {
+ 
+ /* Elegant inner border frame for the map */
+ #mapArea::before {
+-  content: '';
+-  position: absolute;
+-  inset: 10px 10px 56px 10px;
+-  border: 1px solid rgba(168,116,32,0.35);
+-  border-radius: 20px;
+-  box-shadow:
+-    inset 0 0 0 1px rgba(255,245,210,0.5),
+-    0 0 30px rgba(168,116,32,0.08);
+-  pointer-events: none;
+-  z-index: 1;
++  content: none;
+ }
+ 
+ #mapContainer {
+   width: 100%;
+   height: 100%;
+   min-width: 0;
+-  padding: clamp(10px, 1.4vw, 18px) clamp(10px, 1.4vw, 18px) calc(58px + env(safe-area-inset-bottom));
++  padding: var(--map-shell-pad) var(--map-shell-pad) var(--map-reserve-bottom);
+   display: flex;
+   align-items: center;
+   justify-content: center;
+@@ -330,11 +325,12 @@ body::before {
+ 
+ .game-map {
+   display: block;
+-  width: 100%;
++  width: auto;
+   height: 100%;
+-  min-width: 0;
+-  max-width: 100%;
++  aspect-ratio: var(--map-aspect);
++  max-width: min(100%, 1120px);
+   max-height: 100%;
++  border: 1px solid rgba(100,70,20,0.14);
+   border-radius: 16px;
+   background: linear-gradient(160deg, rgba(230,240,248,0.85) 0%, rgba(220,210,180,0.80) 100%);
+   box-shadow:
+@@ -352,8 +348,8 @@ body::before {
+   left: 0;
+   right: 0;
+   display: flex;
+-  gap: 4px;
+-  padding: 6px max(8px, env(safe-area-inset-left)) calc(7px + env(safe-area-inset-bottom)) max(8px, env(safe-area-inset-right));
++  gap: 8px;
++  padding: 10px max(14px, env(safe-area-inset-left)) calc(12px + env(safe-area-inset-bottom)) max(14px, env(safe-area-inset-right));
+   overflow-x: auto;
+   overflow-y: hidden;
+   scrollbar-width: none;
+@@ -366,12 +362,12 @@ body::before {
+ /* Player tabs reuse the cartouche grammar at a larger scale: thick role
+    outline, dynasty-color fill, parchment ink, gold-leaf inner hairline. */
+ .player-tab {
+-  flex: 1 0 clamp(132px, 19vw, 190px);
++  flex: 1 0 clamp(156px, 18vw, 220px);
+   min-width: 0;
+   display: flex;
+   align-items: center;
+   gap: var(--sp-2);
+-  padding: 6px 12px 7px;
++  padding: 8px 14px 9px;
+   border: 4px solid var(--role-outline-color, var(--role-color, var(--empire-border)));
+   border-bottom-width: 0;
+   border-radius: var(--r-sm) var(--r-sm) 0 0;
+@@ -455,9 +451,12 @@ body::before {
+   flex-direction: column;
+   overflow-y: auto;
+   overflow-x: hidden;
++  gap: var(--sidebar-gap);
++  padding: var(--sidebar-gap);
+   background:
+     linear-gradient(180deg, var(--parch-0) 0%, var(--parch-1) 100%);
+   border-left: 1px solid var(--parch-3);
++  scrollbar-gutter: stable;
+   min-width: 0;
+   min-height: 0;
+   box-shadow: -8px 0 24px rgba(80,50,10,0.10);
+@@ -492,8 +491,11 @@ body::before {
+ .sidebar-panel {
+   display: flex;
+   flex-direction: column;
+-  background: transparent;
++  background: rgba(255,252,240,0.54);
+   overflow: visible;
++  border: 1px solid rgba(168,116,32,0.16);
++  border-radius: var(--r-lg);
++  box-shadow: var(--shadow-card);
+ }
+ 
+ .sidebar-panel-head {
+@@ -598,7 +600,7 @@ body::before {
+     ),
+     linear-gradient(180deg, rgba(255,248,220,0.18) 0%, rgba(20,8,0,0.18) 100%);
+   border: 4px solid var(--role-outline-color, var(--role-color, var(--empire-border)));
+-  border-radius: 3px;
++  border-radius: var(--r-lg);
+   box-shadow:
+     inset 0 0 0 1px rgba(240,200,90,0.45),
+     inset 0 1px 0 rgba(255,250,228,0.18),
+@@ -740,14 +742,14 @@ body::before {
+ 
+ .dashboard-stats {
+   display: grid;
+-  grid-template-columns: repeat(auto-fit, minmax(88px, 1fr));
++  grid-template-columns: repeat(auto-fit, minmax(112px, 1fr));
+   gap: var(--sp-2);
+   margin-bottom: var(--sp-3);
+ }
+ 
+ .stat {
+   flex: 1;
+-  padding: var(--sp-2) var(--sp-2);
++  padding: 10px var(--sp-3);
+   border-radius: var(--r-sm);
+   text-align: center;
+   background: rgba(0,0,0,0.1);
+@@ -807,7 +809,7 @@ body::before {
+   box-shadow: var(--shadow-card);
+ }
+ 
+-.fold-section + .fold-section { margin-top: var(--sp-2); }
++.fold-section + .fold-section { margin-top: var(--sp-3); }
+ .fold-section.focused { border-color: var(--parch-3); }
+ 
+ .fold-summary {
+@@ -854,7 +856,7 @@ body::before {
+ .dashboard-list {
+   display: flex;
+   flex-direction: column;
+-  gap: 6px;
++  gap: 8px;
+ }
+ 
+ .dashboard-list.compact { margin-top: var(--sp-2); }
+@@ -865,8 +867,8 @@ body::before {
+   align-items: flex-start;
+   justify-content: space-between;
+   gap: var(--sp-2);
+-  padding: 7px 10px;
+-  border-radius: var(--r-sm);
++  padding: 9px 12px;
++  border-radius: var(--r-md);
+   background: rgba(255,252,240,0.85);
+   border: 1px solid rgba(168,116,32,0.14);
+ }
+@@ -942,7 +944,7 @@ body::before {
+ /* Finance cards */
+ .finance-grid {
+   display: grid;
+-  grid-template-columns: repeat(auto-fit, minmax(min(100%, 136px), 1fr));
++  grid-template-columns: repeat(auto-fit, minmax(min(100%, 158px), 1fr));
+   gap: var(--sp-2);
+   margin-bottom: var(--sp-2);
+ }
+@@ -1035,7 +1037,7 @@ body::before {
+ .theme-market,
+ .gift-options {
+   display: grid;
+-  grid-template-columns: repeat(auto-fit, minmax(min(100%, 148px), 1fr));
++  grid-template-columns: repeat(auto-fit, minmax(min(100%, 176px), 1fr));
+   gap: var(--sp-2);
+ }
+ 
+@@ -1094,7 +1096,7 @@ body::before {
+ 
+ 
+ /* ── Appointment blocks ──────────────────────────────────────────── */
+-.appointment-block { margin-bottom: var(--sp-3); }
++.appointment-block { margin-bottom: var(--sp-4); }
+ .appointment-block.done { opacity: 0.65; }
+ .appointment-block.done .appt-label { color: var(--gold-2); }
+ 
+@@ -1109,12 +1111,12 @@ body::before {
+ .appt-form {
+   display: flex;
+   flex-direction: column;
+-  gap: var(--sp-1);
++  gap: var(--sp-2);
+ }
+ 
+ .appt-grid {
+   display: grid;
+-  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
++  grid-template-columns: repeat(2, minmax(150px, 1fr));
+   gap: var(--sp-2);
+   align-items: start;
+ }
+@@ -1142,7 +1144,7 @@ body::before {
+ .appt-select,
+ .revoke-select {
+   width: 100%;
+-  padding: 7px 10px;
++  padding: 9px 12px;
+   background: var(--parch-0);
+   border: 1px solid var(--parch-2);
+   border-radius: var(--r-sm);
+@@ -1218,6 +1220,8 @@ body::before {
+ 
+ .army-office {
+   display: flex;
++  flex: 1 1 160px;
++  min-width: 0;
+   flex-direction: column;
+   gap: 2px;
+ }
+@@ -1293,7 +1297,7 @@ body::before {
+   align-items: center;
+   flex-wrap: wrap;
+   gap: var(--sp-2);
+-  padding: 7px 10px;
++  padding: 9px 12px;
+   background: var(--parch-0);
+   border: 1px solid var(--parch-2);
+   border-radius: var(--r-sm);
+@@ -1355,8 +1359,9 @@ body::before {
+ .merc-row {
+   display: flex;
+   align-items: center;
++  flex-wrap: wrap;
+   gap: var(--sp-2);
+-  padding: 6px 10px;
++  padding: 9px 12px;
+   background: var(--parch-0);
+   border: 1px solid var(--parch-2);
+   border-radius: var(--r-sm);
+@@ -1368,6 +1373,7 @@ body::before {
+ .merc-controls {
+   margin-left: auto;
+   display: flex;
++  flex: 0 1 auto;
+   align-items: center;
+   gap: 7px;
+ }
+@@ -1407,13 +1413,14 @@ body::before {
+ .merc-total .over-budget { color: var(--crimson-1); font-weight: 600; }
+ 
+ /* Candidate buttons — share the cartouche grammar at row scale. */
+-.candidate-grid { display: flex; flex-direction: column; gap: var(--sp-1); }
++.candidate-grid { display: flex; flex-direction: column; gap: var(--sp-2); }
+ 
+ .candidate-btn {
+   display: flex;
+   align-items: center;
++  flex-wrap: wrap;
+   gap: var(--sp-2);
+-  padding: 7px 12px 8px;
++  padding: 10px 14px 11px;
+   background-color: var(--player-color, var(--parch-3));
+   background-image:
+     repeating-linear-gradient(
+@@ -1463,6 +1470,8 @@ body::before {
+ 
+ .current-basileus-tag {
+   margin-left: auto;
++  white-space: normal;
++  text-align: right;
+   font-size: 9px;
+   color: var(--gold-2);
+   font-family: var(--font-display);
+@@ -1691,7 +1700,7 @@ body::before {
+ .history-panel-list {
+   display: flex;
+   flex-direction: column;
+-  gap: var(--sp-2);
++  gap: var(--sp-3);
+   padding: var(--sp-3);
+ }
+ 
+@@ -1855,7 +1864,7 @@ body::before {
+ }
+ 
+ /* Theme chips */
+-.dashboard-themes { display: flex; flex-wrap: wrap; gap: var(--sp-1); }
++.dashboard-themes { display: flex; flex-wrap: wrap; gap: var(--sp-2); }
+ .theme-chip {
+   padding: 2px 9px;
+   background: var(--parch-0);
+@@ -2224,7 +2233,7 @@ body::before {
+ .province-choice-grid {
+   display: flex;
+   flex-wrap: wrap;
+-  gap: var(--sp-1);
++  gap: var(--sp-2);
+ }
+ 
+ .player-choice-btn,
+@@ -2265,7 +2274,7 @@ body::before {
+ /* Revocation choice grid — same cartouche pattern as player/province
+    choice grids. Each button wraps one or two cartouches that describe
+    what the revocation will affect. */
+-.revocation-shell { display: flex; flex-direction: column; gap: var(--sp-2); margin-top: 4px; }
++.revocation-shell { display: flex; flex-direction: column; gap: var(--sp-3); margin-top: 6px; }
+ .revocation-group { display: flex; flex-direction: column; gap: 4px; }
+ .revocation-group-label {
+   font-family: var(--font-display);
+@@ -2278,7 +2287,7 @@ body::before {
+ .revocation-choice-grid {
+   display: flex;
+   flex-wrap: wrap;
+-  gap: var(--sp-1);
++  gap: var(--sp-2);
+ }
+ .revocation-choice-btn {
+   display: inline-flex;
+@@ -2655,6 +2664,91 @@ body::before {
+ 
+ 
+ 
++
++/* ── Breathing layout pass ──────────────────────────────────────────
++   Keeps the SVG canvas locked to the map aspect ratio, widens the usable
++   interface column, and gives repeated controls/cards consistent space. */
++@media (min-width: 981px) {
++  #mapContainer {
++    justify-content: center;
++  }
++
++  .sidebar-panel + .sidebar-panel,
++  #historyPanel + #actionPanel {
++    margin-top: 0;
++  }
++}
++
++.sidebar-panel-head {
++  min-height: var(--touch-target);
++}
++
++.sidebar-panel-badge,
++.dashboard-list-value,
++.score-breakdown,
++.current-basileus-tag {
++  max-width: 48%;
++}
++
++.fold-body,
++.court-section,
++.orders-section,
++.resolution-section,
++.phase-header,
++.court-actions,
++.orders-actions,
++.resolution-actions {
++  min-width: 0;
++}
++
++.deploy-row {
++  display: grid;
++  grid-template-columns: minmax(140px, 1fr) auto auto;
++  align-items: center;
++}
++
++.deploy-toggle {
++  min-width: min(100%, 150px);
++}
++
++.toggle-btn,
++.merc-btn,
++.btn-recruit,
++.btn-dismiss,
++.btn-seat-kind,
++#btnLeaveRoom,
++.inline-button {
++  min-height: 36px;
++}
++
++.market-item,
++.gift-item,
++.candidate-btn,
++.deploy-row,
++.merc-row,
++.history-entry-card,
++.dashboard-list-row,
++.dashboard-opinion-row,
++.finance-card,
++.theme-chip,
++.score-row {
++  min-width: 0;
++}
++
++@media (max-width: 1180px) and (min-width: 981px) {
++  #app {
++    grid-template-columns: minmax(420px, 1fr) minmax(380px, var(--sidebar-width));
++  }
++
++  .deploy-row {
++    grid-template-columns: minmax(0, 1fr);
++  }
++
++  .deploy-toggle {
++    width: 100%;
++  }
++}
++
+ /* ── Responsive Ergonomics & Firefox/Mobile Compatibility ─────────────
+    The desktop game stays two-column. Narrow screens become a scrollable
+    map-first document so the sidebar no longer crushes the map. */
+@@ -2707,13 +2801,13 @@ select,
+ input,
+ summary { touch-action: manipulation; }
+ 
+-@media (max-width: 1120px) {
+-  :root { --sidebar-width: clamp(320px, 34vw, 380px); }
++@media (max-width: 1180px) {
++  :root { --sidebar-width: clamp(380px, 38vw, 460px); --main-panel-pad: 16px; }
+   .game-title { letter-spacing: 3px; }
+   .phase-badge { padding-inline: 10px; }
+ }
+ 
+-@media (max-width: 900px) {
++@media (max-width: 980px) {
+   body {
+     overflow: auto;
+     min-height: var(--game-vh);
+@@ -2722,7 +2816,7 @@ summary { touch-action: manipulation; }
+ 
+   #app {
+     grid-template-columns: minmax(0, 1fr);
+-    grid-template-rows: auto clamp(340px, 54svh, 580px) auto;
++    grid-template-rows: auto clamp(330px, 50svh, 540px) auto;
+     grid-template-areas:
+       "topbar"
+       "map"
+@@ -2749,7 +2843,7 @@ summary { touch-action: manipulation; }
+   }
+ 
+   #mapArea {
+-    min-height: 340px;
++    min-height: 320px;
+     border-bottom: 1px solid var(--parch-3);
+   }
+ 
+@@ -2758,6 +2852,7 @@ summary { touch-action: manipulation; }
+     border-left: 0;
+     border-top: 1px solid var(--parch-3);
+     box-shadow: 0 -8px 24px rgba(80,50,10,0.08);
++    padding: var(--main-panel-pad);
+   }
+ 
+   #playerDashboard,
+@@ -2778,7 +2873,7 @@ summary { touch-action: manipulation; }
+   }
+ 
+   #app {
+-    grid-template-rows: auto clamp(320px, 50svh, 520px) auto;
++    grid-template-rows: auto clamp(300px, 46svh, 500px) auto;
+   }
+ 
+   #topBar {
+@@ -2806,14 +2901,14 @@ summary { touch-action: manipulation; }
+     white-space: normal;
+   }
+ 
+-  #mapArea::before { inset: 8px 8px 54px; border-radius: 14px; }
+-  #mapContainer { padding: 10px 10px calc(56px + env(safe-area-inset-bottom)); }
++  #mapArea::before { inset: 10px 10px calc(68px + env(safe-area-inset-bottom)); border-radius: 14px; }
++  #mapContainer { padding: 10px 10px calc(72px + env(safe-area-inset-bottom)); }
+   .game-map { border-radius: 12px; }
+ 
+-  #playerTabBar { gap: 5px; }
++  #playerTabBar { gap: 7px; padding-top: 9px; }
+   .player-tab {
+-    flex-basis: 145px;
+-    padding: 7px 10px;
++    flex-basis: 156px;
++    padding: 8px 10px;
+     font-size: 11px;
+     border-width: 3px;
+     border-bottom-width: 0;
+@@ -2931,7 +3026,7 @@ summary { touch-action: manipulation; }
+ }
+ 
+ @media (max-width: 480px) {
+-  #app { grid-template-rows: auto clamp(300px, 48svh, 480px) auto; }
++  #app { grid-template-rows: auto clamp(280px, 44svh, 440px) auto; }
+   .game-title { flex-basis: 100%; }
+   #roundDisplay { margin-right: auto; }
+   .phase-badge { padding: 4px 8px; }
+@@ -2952,6 +3047,42 @@ summary { touch-action: manipulation; }
+   .scoring-panel { padding: var(--main-panel-pad); }
+ }
+ 
++
++@media (max-width: 720px) {
++  #sidebar {
++    gap: var(--sp-3);
++  }
++
++  .sidebar-panel {
++    border-radius: var(--r-md);
++  }
++
++  .sidebar-panel-badge,
++  .dashboard-list-value,
++  .score-breakdown,
++  .current-basileus-tag {
++    max-width: 100%;
++  }
++
++  .deploy-row {
++    grid-template-columns: 1fr;
++  }
++
++  .merc-controls,
++  .deploy-toggle {
++    justify-content: stretch;
++  }
++
++  .merc-btn,
++  .merc-count {
++    flex: 1;
++  }
++
++  .market-cost {
++    margin-left: 0;
++  }
++}
++
+ /* ── Scrollbar (global) ──────────────────────────────────────────── */
+ ::-webkit-scrollbar { width: 7px; }
+ ::-webkit-scrollbar-track { background: var(--parch-1); }
-- 
2.47.3


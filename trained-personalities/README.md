# Trained Personalities

This directory is reserved for generated AI policy champion exports.

The old profile-weight rosters were removed during the policy-genome cleanup. Run:

```powershell
npm.cmd run train:node
```

The trainer recreates `latest/` and `runs/` with fresh policy-only champions.

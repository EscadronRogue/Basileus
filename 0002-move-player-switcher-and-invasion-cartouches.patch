From 36d97e7f4c3d209bad2c340aa0efa583296d058f Mon Sep 17 00:00:00 2001
From: "GPT-5.5 Thinking" <assistant@example.com>
Date: Sun, 3 May 2026 20:18:41 +0000
Subject: [PATCH] Move player switcher and restyle invasion labels

---
 PLAYER_SWITCHER_INVASION_CARTOUCHE_PASS.md | 28 +++++++
 assets/style.css                           | 97 +++++++++++++++-------
 index.html                                 |  2 +-
 render/mapRenderer.js                      | 65 ++++++++++++---
 ui/gameController.js                       | 13 +--
 ui/multiplayerController.js                | 11 +--
 6 files changed, 156 insertions(+), 60 deletions(-)
 create mode 100644 PLAYER_SWITCHER_INVASION_CARTOUCHE_PASS.md

diff --git a/PLAYER_SWITCHER_INVASION_CARTOUCHE_PASS.md b/PLAYER_SWITCHER_INVASION_CARTOUCHE_PASS.md
new file mode 100644
index 0000000..51da96e
--- /dev/null
+++ b/PLAYER_SWITCHER_INVASION_CARTOUCHE_PASS.md
@@ -0,0 +1,28 @@
+# Player Switcher and Invasion Cartouche Pass
+
+## Intent
+
+This pass moves the player selection cartouches out of the map footer and into the top of the interface column. It also gives the interface more horizontal room and makes invasion information live on the map at the invasion origin instead of in the top bar.
+
+## Changes
+
+- Moved `#playerTabBar` from `#mapArea` into `#sidebar` above the dashboard.
+- Converted the player selector from a bottom-pinned map footer into a sticky interface header.
+- Restored complete cartouche outlines around player selector buttons.
+- Removed the bottom map-space reservation that existed only to protect the old footer tabs.
+- Widened the interface column again on desktop and medium screens.
+- Removed top-bar invasion text to avoid redundant information.
+- Replaced the old single-line SVG invasion label with a two-line cartouche at the invasion origin:
+  - line 1: invasion name
+  - line 2: strength estimate
+- Standardized invasion route, markers, cartouche border, and cartouche text to the occupied/lost-province color system.
+
+## Validation
+
+- `node --check render/mapRenderer.js`
+- `node --check ui/gameController.js`
+- `node --check ui/multiplayerController.js`
+- `npm test`
+- CSS brace balance check
+
+Browser screenshot validation could not run in this container because Chromium blocks local HTTP/file navigation by administrator policy.
diff --git a/assets/style.css b/assets/style.css
index 403e0cd..d12e959 100644
--- a/assets/style.css
+++ b/assets/style.css
@@ -83,9 +83,9 @@
 
   /* Responsive shell */
   --topbar-height: 52px;
-  --sidebar-width: clamp(410px, 34vw, 560px);
+  --sidebar-width: clamp(440px, 37vw, 640px);
   --map-aspect: 297 / 210;
-  --map-reserve-bottom: calc(76px + env(safe-area-inset-bottom));
+  --map-reserve-bottom: var(--map-shell-pad);
   --map-shell-pad: clamp(14px, 1.8vw, 24px);
   --sidebar-gap: clamp(12px, 1.3vw, 18px);
   --main-panel-pad: clamp(16px, 1.7vw, 24px);
@@ -113,6 +113,8 @@
   --text-gold:     var(--gold-1);
   --occupied:      var(--occupied-fill);
   --occupied-border: #9a8060;
+  --invasion-lost-fill: rgba(210, 200, 178, 0.92);
+  --invasion-lost-border: var(--occupied-border);
   --region-border: var(--empire-border);
 }
 
@@ -164,7 +166,7 @@ body::before {
   z-index: 1;
   display: grid;
   grid-template-rows: var(--topbar-height) minmax(0, 1fr);
-  grid-template-columns: minmax(460px, 1fr) minmax(390px, var(--sidebar-width));
+  grid-template-columns: minmax(420px, 1fr) minmax(430px, var(--sidebar-width));
   grid-template-areas:
     "topbar topbar"
     "map    sidebar";
@@ -343,34 +345,35 @@ body::before {
 
 /* ── Player Tabs ─────────────────────────────────────────────────── */
 #playerTabBar {
-  position: absolute;
-  bottom: 0;
-  left: 0;
-  right: 0;
+  position: sticky;
+  top: 0;
+  z-index: 6;
+  flex: 0 0 auto;
   display: flex;
-  gap: 8px;
-  padding: 10px max(14px, env(safe-area-inset-left)) calc(12px + env(safe-area-inset-bottom)) max(14px, env(safe-area-inset-right));
+  gap: 10px;
+  padding: clamp(12px, 1.5vw, 18px) var(--main-panel-pad);
   overflow-x: auto;
   overflow-y: hidden;
   scrollbar-width: none;
-  background: linear-gradient(180deg, rgba(240,228,198,0.92) 0%, rgba(220,205,168,0.97) 100%);
-  border-top: 1px solid rgba(168,116,32,0.35);
+  background:
+    linear-gradient(180deg, rgba(255,250,235,0.98) 0%, rgba(240,228,198,0.96) 100%);
+  border-bottom: 1px solid rgba(168,116,32,0.30);
+  box-shadow: 0 5px 16px rgba(80,50,10,0.08);
   backdrop-filter: blur(8px);
-  z-index: 2;
+  -webkit-backdrop-filter: blur(8px);
 }
 
 /* Player tabs reuse the cartouche grammar at a larger scale: thick role
    outline, dynasty-color fill, parchment ink, gold-leaf inner hairline. */
 .player-tab {
-  flex: 1 0 clamp(156px, 18vw, 220px);
+  flex: 1 0 clamp(158px, 48%, 240px);
   min-width: 0;
   display: flex;
   align-items: center;
   gap: var(--sp-2);
-  padding: 8px 14px 9px;
+  padding: 9px 14px 10px;
   border: 4px solid var(--role-outline-color, var(--role-color, var(--empire-border)));
-  border-bottom-width: 0;
-  border-radius: var(--r-sm) var(--r-sm) 0 0;
+  border-radius: var(--r-md);
   background-color: var(--player-color, var(--parch-3));
   background-image:
     repeating-linear-gradient(
@@ -391,7 +394,7 @@ body::before {
     inset 0 0 0 1px rgba(240,200,90,0.45),
     inset 0 1px 0 rgba(255,250,228,0.18),
     inset 0 -1px 0 rgba(20,8,0,0.22),
-    0 -2px 6px rgba(30,16,0,0.10);
+    0 2px 8px rgba(30,16,0,0.12);
 }
 
 #playerTabBar::-webkit-scrollbar { display: none; }
@@ -404,7 +407,7 @@ body::before {
     inset 0 0 0 1px rgba(240,200,90,0.70),
     inset 0 1px 0 rgba(255,250,228,0.22),
     inset 0 -1px 0 rgba(20,8,0,0.22),
-    0 -4px 10px rgba(30,16,0,0.18);
+    0 3px 12px rgba(30,16,0,0.18);
 }
 
 .player-tab,
@@ -2056,21 +2059,58 @@ body::before {
 /* Invasion route */
 .invasion-route {
   fill: none;
+  stroke: var(--invasion-lost-border);
   stroke-width: 0.7;
   stroke-dasharray: 1.8 1.2;
-  opacity: 0.75;
+  opacity: 0.82;
   animation: marchingAnts 1s linear infinite;
 }
 @keyframes marchingAnts { to { stroke-dashoffset: -3; } }
 
-.invasion-marker { opacity: 0.65; }
+.invasion-marker {
+  fill: var(--invasion-lost-border);
+  opacity: 0.78;
+}
 
-.invasion-label {
-  font-family: var(--font-display);
-  font-size: 2.5px;
+.invasion-cartouche {
+  pointer-events: none;
+}
+
+.invasion-cartouche-bg {
+  fill: var(--invasion-lost-fill);
+  stroke: var(--invasion-lost-border);
+  stroke-width: 0.52;
+  paint-order: stroke fill;
+}
+
+.invasion-cartouche-inner {
+  fill: none;
+  stroke: rgba(255,250,228,0.64);
+  stroke-width: 0.12;
+}
+
+.invasion-cartouche-name,
+.invasion-cartouche-strength {
   text-anchor: middle;
+  dominant-baseline: middle;
+  fill: var(--invasion-lost-border);
+  paint-order: stroke fill;
+  stroke: rgba(255,250,228,0.46);
+  stroke-width: 0.08;
+  pointer-events: none;
+}
+
+.invasion-cartouche-name {
+  font-family: var(--font-display);
+  font-size: 2.15px;
+  font-weight: 700;
+  letter-spacing: 0.12px;
+}
+
+.invasion-cartouche-strength {
+  font-family: var(--font-body);
+  font-size: 1.72px;
   font-weight: 600;
-  fill: var(--gold-1);
 }
 
 .province-threat-overlay {
@@ -2737,7 +2777,7 @@ body::before {
 
 @media (max-width: 1180px) and (min-width: 981px) {
   #app {
-    grid-template-columns: minmax(420px, 1fr) minmax(380px, var(--sidebar-width));
+    grid-template-columns: minmax(390px, 1fr) minmax(420px, var(--sidebar-width));
   }
 
   .deploy-row {
@@ -2802,7 +2842,7 @@ input,
 summary { touch-action: manipulation; }
 
 @media (max-width: 1180px) {
-  :root { --sidebar-width: clamp(380px, 38vw, 460px); --main-panel-pad: 16px; }
+  :root { --sidebar-width: clamp(400px, 42vw, 520px); --main-panel-pad: 16px; }
   .game-title { letter-spacing: 3px; }
   .phase-badge { padding-inline: 10px; }
 }
@@ -2902,16 +2942,15 @@ summary { touch-action: manipulation; }
   }
 
   #mapArea::before { inset: 10px 10px calc(68px + env(safe-area-inset-bottom)); border-radius: 14px; }
-  #mapContainer { padding: 10px 10px calc(72px + env(safe-area-inset-bottom)); }
+  #mapContainer { padding: 10px; }
   .game-map { border-radius: 12px; }
 
-  #playerTabBar { gap: 7px; padding-top: 9px; }
+  #playerTabBar { gap: 8px; padding-top: 12px; padding-bottom: 12px; }
   .player-tab {
     flex-basis: 156px;
     padding: 8px 10px;
     font-size: 11px;
     border-width: 3px;
-    border-bottom-width: 0;
   }
   .tab-crest { width: 20px; height: 20px; }
   .tab-gold { margin-left: 0; }
diff --git a/index.html b/index.html
index 9a32da2..1290280 100644
--- a/index.html
+++ b/index.html
@@ -30,11 +30,11 @@
     <!-- Map Area -->
     <main id="mapArea">
       <div id="mapContainer"></div>
-      <div id="playerTabBar"></div>
     </main>
 
     <!-- Sidebar: Dashboard + Action Panel -->
     <aside id="sidebar">
+      <div id="playerTabBar" aria-label="Player selection"></div>
       <div id="playerDashboard"></div>
       <div id="historyPanel"></div>
       <div id="actionPanel"></div>
diff --git a/render/mapRenderer.js b/render/mapRenderer.js
index 2f87c34..f26cd21 100644
--- a/render/mapRenderer.js
+++ b/render/mapRenderer.js
@@ -599,29 +599,72 @@ export function drawInvasionRoute(invasion) {
     pathData += ` L ${points[index].cx} ${points[index].cy}`;
   }
 
-  const path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
+  const path = document.createElementNS(SVG_NS, 'path');
   path.setAttribute('d', pathData);
   path.setAttribute('class', 'invasion-route');
-  path.style.stroke = invasion.color;
   layer.appendChild(path);
 
   for (let index = 1; index < points.length; index += 1) {
-    const marker = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
+    const marker = document.createElementNS(SVG_NS, 'circle');
     marker.setAttribute('cx', points[index].cx);
     marker.setAttribute('cy', points[index].cy);
     marker.setAttribute('r', 0.8);
     marker.setAttribute('class', 'invasion-marker');
-    marker.style.fill = invasion.color;
     layer.appendChild(marker);
   }
 
-  const label = document.createElementNS('http://www.w3.org/2000/svg', 'text');
-  label.setAttribute('x', points[0].cx);
-  label.setAttribute('y', points[0].cy - 1.5);
-  label.setAttribute('class', 'invasion-label');
-  label.style.fill = invasion.color;
-  label.textContent = invasion.name;
-  layer.appendChild(label);
+  appendInvasionCartouche(layer, invasion, points[0]);
+}
+
+function appendInvasionCartouche(layer, invasion, point) {
+  if (!point) return;
+
+  const strengthText = Array.isArray(invasion.strength) && invasion.strength.length === 2
+    ? `Strength ${invasion.strength[0]}-${invasion.strength[1]}`
+    : 'Strength ?';
+  const nameText = invasion.name || 'Invasion';
+  const width = Math.max(26, Math.min(44, Math.max(nameText.length, strengthText.length) * 1.45 + 7));
+  const height = 9.6;
+  const x = clampValue(point.cx, (width / 2) + 1.2, 297 - (width / 2) - 1.2);
+  const y = clampValue(point.cy - 7.3, 1.2, 210 - height - 1.2);
+
+  const group = document.createElementNS(SVG_NS, 'g');
+  group.setAttribute('class', 'invasion-cartouche');
+  group.setAttribute('transform', `translate(${x - (width / 2)} ${y})`);
+
+  const bg = document.createElementNS(SVG_NS, 'rect');
+  bg.setAttribute('class', 'invasion-cartouche-bg');
+  bg.setAttribute('width', width.toFixed(2));
+  bg.setAttribute('height', height.toFixed(2));
+  bg.setAttribute('rx', '1.6');
+  bg.setAttribute('ry', '1.6');
+  group.appendChild(bg);
+
+  const inner = document.createElementNS(SVG_NS, 'rect');
+  inner.setAttribute('class', 'invasion-cartouche-inner');
+  inner.setAttribute('x', '0.8');
+  inner.setAttribute('y', '0.8');
+  inner.setAttribute('width', (width - 1.6).toFixed(2));
+  inner.setAttribute('height', (height - 1.6).toFixed(2));
+  inner.setAttribute('rx', '1.1');
+  inner.setAttribute('ry', '1.1');
+  group.appendChild(inner);
+
+  const name = document.createElementNS(SVG_NS, 'text');
+  name.setAttribute('class', 'invasion-cartouche-name');
+  name.setAttribute('x', (width / 2).toFixed(2));
+  name.setAttribute('y', '3.75');
+  name.textContent = nameText;
+  group.appendChild(name);
+
+  const strength = document.createElementNS(SVG_NS, 'text');
+  strength.setAttribute('class', 'invasion-cartouche-strength');
+  strength.setAttribute('x', (width / 2).toFixed(2));
+  strength.setAttribute('y', '7.15');
+  strength.textContent = strengthText;
+  group.appendChild(strength);
+
+  layer.appendChild(group);
 }
 
 export function getCentroids() {
diff --git a/ui/gameController.js b/ui/gameController.js
index 1c4b024..b27d530 100644
--- a/ui/gameController.js
+++ b/ui/gameController.js
@@ -347,17 +347,10 @@ export class GameController {
     }
     this.renderEmpireFallenBanner();
 
-    if (!invasionEl) return;
-    if (state.currentInvasion) {
-      const invasion = state.currentInvasion;
-      invasionEl.innerHTML = `
-        <span class="invasion-name" style="color:${invasion.color}">War: ${invasion.name}</span>
-        <span class="invasion-strength">Est. ${invasion.strength[0]}-${invasion.strength[1]}</span>
-      `;
-      invasionEl.style.display = '';
-      return;
+    if (invasionEl) {
+      invasionEl.textContent = '';
+      invasionEl.style.display = 'none';
     }
-    invasionEl.style.display = 'none';
   }
 
   renderEmpireFallenBanner() {
diff --git a/ui/multiplayerController.js b/ui/multiplayerController.js
index 46c95de..a422614 100644
--- a/ui/multiplayerController.js
+++ b/ui/multiplayerController.js
@@ -488,15 +488,8 @@ export class MultiplayerController {
     }
     this.renderEmpireFallenBanner();
 
-    if (!invasionEl) return;
-    if (this.state.currentInvasion) {
-      const invasion = this.state.currentInvasion;
-      invasionEl.innerHTML = `
-        <span class="invasion-name" style="color:${invasion.color}">War: ${invasion.name}</span>
-        <span class="invasion-strength">Est. ${invasion.strength[0]}-${invasion.strength[1]}</span>
-      `;
-      invasionEl.style.display = '';
-    } else {
+    if (invasionEl) {
+      invasionEl.textContent = '';
       invasionEl.style.display = 'none';
     }
   }
-- 
2.47.3

